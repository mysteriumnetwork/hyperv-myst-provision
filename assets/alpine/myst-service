#!/sbin/openrc-run
name="Mysterium Network Node"
command="myst-wrapper.sh"
command_args="--keystore.lightweight service --agreed-terms-and-conditions wireguard"
command_background=true
pidfile="/run/${RC_SVCNAME}.pid"

depend() {
        need net
}